@* jQuery validation scripts are already loaded globally in _Layout.cshtml.
   This partial exists only so @section Scripts { <partial name="_ValidationScriptsPartial" /> }
   references keep working without any extra script tags being duplicated. *@
